services:
  - type: web
    name: luma-scraper
    runtime: docker
    repo: https://github.com/ShonZimkov/luma-scraper-clean
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
